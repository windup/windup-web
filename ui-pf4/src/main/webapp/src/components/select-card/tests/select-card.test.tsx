import React from "react";
import { shallow } from "enzyme";
import { SelectCard } from "../select-card";

describe("SelectCard", () => {
  it("Renders without crashing", () => {
    // const wrapper = shallow(<SelectCard />);
    // expect(wrapper).toMatchSnapshot();
  });
});
