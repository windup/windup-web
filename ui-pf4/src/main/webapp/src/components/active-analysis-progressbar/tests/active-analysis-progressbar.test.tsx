import React from "react";
import { shallow } from "enzyme";
import { ActiveAnalysisProgressbar } from "../active-analysis-progressbar";

describe("ActiveAnalysisProgressbar", () => {
  it("Renders without crashing", () => {
    // const wrapper = shallow(<ActiveAnalysisProgressbar />);
    // expect(wrapper).toMatchSnapshot();
  });
});
