import * as React from "react";
import { shallow } from "enzyme";
import { SelectCardGallery } from "../select-card-gallery";

describe("SelectCard", () => {
  it("Renders without crashing", () => {
    // const wrapper = shallow(<SelectCard />);
    // expect(wrapper).toMatchSnapshot();
  });
});
